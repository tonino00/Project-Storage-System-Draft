<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Projetos</title>
</head>
<body>
    <div class="container">
       <h1 class="mb-2"><%= article.title %></h1>
       <div class="card-subtitle text-muted">
        Autor: <%= article.fullname %>
        </div>
       <div class="text-muted mb-2">
          Cadastro em: <%= article.createdAt.toLocaleDateString() %>
        </div>
        <div class="mt-2">
            <strong>Descrição:</strong> <%= article.description %>
        </div>
        <div class="mt-2">
            <strong>Viabilidade:</strong> <%= article.viabilidade %>
        </div>
        <div class="mt-2">
            <strong>Valor do Projeto:</strong> R$<%= article.price %>
        </div>
        <div class="mt-2">
            <strong>Situação:</strong> <%= article.situation %>
        </div>
        <div class="mt-2">
            <strong>Data de Início Prevista:</strong> <%= article.startdate %>
        </div>
        <div class="mb-2">
            <strong>Data de Término Prevista:</strong> <%=article.enddate %>
        </div>
        <a href="/" class="btn btn-secondary">Página Inicial</a>
        <a href="/articles/edit/<%= article.id %>" class="btn btn-info"
            <%= article.situation == "Concluído" ? 'hidden' : '' %>
            <%= article.situation == "Cancelado" ? 'hidden' : '' %>>
            Editar
        </a>
    </div>
</body>
</html>